# 系统配置
+ **型号**：FriendlyElec NanoPi R2S
+ **架构**：ARMv8 Processor rev 4 (v8l) x 4
+ **目标平台**：rockchip/armv8
+ **固件版本**：mmortalWrt 23.05.4 r28061-399f9a1db3 / LuCI openwrt-23.05 branch git-24.265.44782-0ff45d8
+ **内核版本**：5.15.167

***
# 1.lan

## 常规设置
+ **协议**：静态地址
+ **设备**：br-lan
+ **禁用该接口**：
+ **开机自动运行**：Y
+ **IPV4子网掩码**:
+ **IPv4 网关**：
+ **IPv4 广播地址**：
+ **IPv6 地址**：
+ **IPv6 网关**：
+ **IPv6 路由前缀**：

## 高级设置
+ **强制链路**：Y
+ **使用默认网关**：Y
+ **使用自定义的 DNS 服务器**：
+ **DNS 搜索域名**：
+ **DNS 权重**：
+ **使用网关跃点**：
+ **覆盖 IPv4 路由表**：未指定
+ **覆盖 IPv6 路由表**：未指定
+ **委托 IPv6 前缀**：
+ **IPv6 分配长度**：已禁用
+ **IPv6 前缀过滤器**：
+ **IPv6 后缀**：
+ **IPv6 优先级**：

## 防火墙设置
+ **创建/分配防火墙区域**：lan

+ ## DHCP服务器
+ ### 常规设置
+ **忽略此接口**：
+ **启动**：100
+ **客户数**：150
+ **租期**：12h
### 高级设置
+ **动态 DHCP**： Y
+ **强制**：
+ **IPv4 子网掩码**：
### DHCP 选项：
+ **IPV6设置**：
+ **指定的主接口**：
+ **RA 服务**：已禁用
+ **DHCPv6 服务**：已禁用
+ **NDP 代理**：已禁用


# 2.wan

## 常规设置
+ **协议**：DHCP客户端
+ **设备**：eth0p [^要注明的文本]：
+ **禁用该接口**：
+ **开机自动运行**：Y
+ **IPV4子网掩码**:
+ **IPv4 网关**：
+ **IPv4 广播地址**：
+ **IPv6 地址**：
+ **IPv6 网关**：
+ **IPv6 路由前缀**：

## 高级设置
+ **强制链路**：Y
+ **使用默认网关**：Y
+ **使用自定义的 DNS 服务器**：
+ **DNS 搜索域名**：
+ **DNS 权重**：
+ **使用网关跃点**：
+ **覆盖 IPv4 路由表**：未指定
+ **覆盖 IPv6 路由表**：未指定
+ **委托 IPv6 前缀**：
+ **IPv6 分配长度**：已禁用
+ **IPv6 前缀过滤器**：
+ **IPv6 后缀**：
+ **IPv6 优先级**：

## 防火墙设置
+ **创建/分配防火墙区域**：lan

+ ## DHCP服务器
+ ### 常规设置
+ **忽略此接口**：
+ **启动**：100
+ **客户数**：150
+ **租期**：12h
### 高级设置
+ **动态 DHCP**： Y
+ **强制**：
+ **IPv4 子网掩码**：
### DHCP 选项：
+ **IPV6设置**：
+ **指定的主接口**：
+ **RA 服务**：已禁用
+ **DHCPv6 服务**：已禁用
+ **NDP 代理**：已禁用



